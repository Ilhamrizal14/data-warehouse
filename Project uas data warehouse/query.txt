#VISUALISASI DATA JUMLAH PEMBELIAN PADA SETIAP BULAN

c.execute('''select * from
(
select a.id_time, count(a.jumlah_pesanan) as jumlah_item, sum(a.jumlah_pesanan) as jumlah_pesanan, b.kontak_person from sales a join kontraktor b
on a.kode_kontraktor = b.kode_kontraktor
group by a.id_time
)''')
jumlah = []
for row in c.fetchall():
    jumlah.append(row)
np_bulan_kontraktor = np.array(jumlah)
[ int (x[0]) for x in np_bulan_kontraktor [:,:]], [ int(x[1]) for x in np_bulan_kontraktor[:,:]], [ int(x[2]) for x in np_bulan_kontraktor[:,:]]